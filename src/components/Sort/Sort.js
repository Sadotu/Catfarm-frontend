import "./Sort.css"
import React from 'react';
import Button from "../Button/Button";

function Sort(props) {
    return (
        <Button
            buttonText="Sort"
            className="filter-sort-button not-allowed"
        ></Button>
    );
}

export default Sort;