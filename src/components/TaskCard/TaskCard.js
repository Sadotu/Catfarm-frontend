import React from 'react';

function TaskCard( { className, cardTitle, deadline, tasks, completed = false } ) {
    return (
        <div className="card-container">

        </div>
    );
}

export default TaskCard;